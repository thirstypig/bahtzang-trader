[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Fast isolated tests (no DB, no network)
    integration: Tests that hit the database (SQLite in-memory)
    e2e: Full API tests via TestClient
