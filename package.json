{
  "name": "bahtzang-trader",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev -- -p 3060",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --port 4060",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && pip install -r requirements.txt",
    "build:frontend": "cd frontend && npm run build",
    "test": "npm run test:backend",
    "test:backend": "cd backend && python -m pytest tests/ -v --tb=short",
    "test:backend:cov": "cd backend && python -m pytest tests/ --cov=app --cov-report=term-missing",
    "test:unit": "cd backend && python -m pytest tests/ -m unit -v",
    "test:integration": "cd backend && python -m pytest tests/ -m integration -v"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
