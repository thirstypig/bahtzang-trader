---
id: DOC-002
type: launch-spec
status: locked
phase: G
owner: james
tags: [foundations, phase-g]
links: [PRD-001, DOC-003]
updated: 2026-07-22
---

# Launch spec

> **Locked for Phase G (paper → live).**
> Changes require the feature-intake process — see `feature-intake-rules.md` (DOC-003).
> "Locked" does not mean finished. It means the scope is closed: anything not on the
> IN list below waits, no matter how small it looks.

<!-- The IN / OUT lists below are deliberately left for James to fill from the phases.
     The archaeology pass (Step 5) reconstructs what the MVP ACTUALLY shipped as, which
     may differ from what was intended — keep both readings if they diverge. That
     divergence is itself the interesting finding. -->

## Scope statement

<!-- One paragraph: what does "launched" mean for this phase? For Phase G the finish
     line is a graduation gate, not a release — state the gate in plain terms. -->

- TODO — define what "launched" means for this phase.

## IN scope

<!-- Only things that must exist for the phase to be declared done. If something can
     slip without blocking the gate, it belongs in OUT or in roadmap.md. -->

- TODO
- TODO
- TODO

## OUT of scope

<!-- Two flavours, worth separating:
       (a) deliberately deferred — decided against for now
       (b) came later / still missing — discovered, not decided -->

### Deliberately deferred

- TODO

### Came later or still missing

- TODO

## Locked decisions

<!-- Decisions that are settled and should not be re-litigated. CLAUDE.md already
     records several for Phase G; link them rather than restating, so there is one
     source of truth. -->

- TODO — link to the Phase G decisions rather than duplicating them.
